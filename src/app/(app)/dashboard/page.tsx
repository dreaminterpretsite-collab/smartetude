import { DashboardClient } from '@/components/dashboard/dashboard-client';

export const metadata = {
  title: 'Tableau de Bord | Smart Études CI',
};

export default function DashboardPage() {
    return (
        <DashboardClient />
    );
}
